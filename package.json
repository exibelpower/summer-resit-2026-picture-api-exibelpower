{
  "name": "picture-sharing-api",
  "version": "1.0.0",
  "private": true,
  "description": "In-memory picture-sharing API for the Noroff course assignment",
  "main": "app.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "jest --runInBand"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^4.21.2",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "jest": "^30.0.5",
    "supertest": "^7.1.4"
  },
  "jest": { "testEnvironment": "node" }
}
